Задача: по готовому диалектическому конспекту (Шаги 1–5) выдать две вещи — короткие заголовки к каждому шагу и исторические справки к тем шагам, где они нужны.

## Заголовки (`titles`) — для КАЖДОГО процесса
Один короткий заголовок на каждый ключ шага. Заголовок — это самая суть того, что в блоке, в одно предложение (можно без глагола, до ~12 слов). По заголовкам одним взглядом должна читаться вся схема ответа. Обычными словами, БЕЗ терминов алгоритма («простейший процесс», «противоречие», «разрешение», «единство» и т.п.). НЕ повторяйте роль шага. Для Шага 4 — что с чем не сходится; для Шага 5 — что именно снимает эту несовместимость.
Пример: `"1": "Выпечка хлеба: мягкий тёплый мякиш из теста"`, `"3": "Разогрев с водой возвращает мякишу мягкость"`.

## Исторические справки (`notes`) — только где нужно
Текст шагов уже ведётся «сначала история, потом логика» (п. 2 главного промпта). Справка нужна НЕ для повтора истории, а для:
- **расхождения логики и истории** (главное) — логический порядок шага заметно отличается от реального хода истории (логика спрямила путь, поменяла порядок, ввела ступень, которой в истории явно не было, — или свернула реальные промежуточные ступени). По п. 2 такие расхождения отмечаются отдельно — здесь;
- **важной исторической детали**, на которую в шаге не хватило места (конкретное имя, дата, работа, условие).
1–3 коротких предложения, только суть. Шаги без справки не включайте. Если справок нет вообще — `notes` пустой объект.
Если предмет — чистая абстракция без внятной истории, дайте ОДНУ справку: коротко скажите, что историческая форма здесь вырождена, и это само по себе расхождение.

## Формат ответа — СТРОГО JSON
```
{
  "titles": {"1": "…", "2.1": "…", "2.2": "…", "3": "…", "4": "…", "5": "…"},
  "notes":  {"2": "…", "4": "…"}
}
```
Ключи в `titles` — ровно те, что есть в конспекте (включая «2.1», «2.2» и т.п.). Ключи в `notes` — только по базовому номеру шага ("1".."5"). Обращение на «вы». Язык — как у конспекта.
