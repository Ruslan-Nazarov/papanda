# Рефакторинг: состояние этапов

## R0 — завершён, 24.09.2026

Основа: аудит от 24.09.2026 для `main@498dc03`.
Рабочая ветка: `refactor/staged-hardening`.

Сделано:

- Ручной режим и поведение onboarding сохранены отдельным коммитом `c88df49`.
- Создано рабочее `.venv` на Python 3.12; старый `venv` сохранён.
- Runtime/dev зависимости разделены; universal locks для Python 3.12 включают
  транзитивные версии, платформенные условия и хеши. Прямые версии приложения
  сохранены; известные advisories закрываются на следующем этапе безопасности.
- Добавлены package.json/package-lock.json с зафиксированным Puppeteer и команды
  проверки JS, логических и браузерных тестов. CDN runtime пока не переносился.
- Добавлены фикстура с настоящим lifespan/get_db и временной SQLite, тест повторного
  открытия engine, регрессии FK/истории, save race и небезопасного DOM-рендера.
- Тесты изолируют пути данных и API-ключи; cleanup фикстур выполняется в finally.
- Добавлен SQLite backup API с проверкой целостности и восстановлением в памяти.
  Локальный снимок: `backups/before-r1-20260924.db`, исключён из Git.
- README, .env.example и run.bat соответствуют новой среде. CI тестов использует
  dev-lock; production-команда установки использует runtime-lock.

Проверено на Windows, Python 3.12.14, Node 24.15.0:

| Проверка | Результат |
|---|---|
| Установка Python по хешам в отдельное окружение | Успешно |
| `pip check` | No broken requirements found |
| `pytest` | 82 passed, 2 xfailed |
| `--runxfail` для SQLite-регрессий | Оба дефекта падают на ожидаемых проверках |
| `npm ci --ignore-scripts` | Успешно, 27 пакетов |
| `npm run check:js` | 56 файлов, ошибок синтаксиса нет |
| `npm test` | 1 passed, 2 todo (известные save race) |
| `npm run test:browser` | Браузерный harness прошёл, 2 todo (известные DOM-инъекции) |
| Снимок текущей БД | Целостность и пробное восстановление проверены |

`xfail`/`todo` — открытые дефекты, не успешные исправления. В R1 убрать каждую
отметку вместе с исправлением. Python xfail строгий: неожиданное прохождение
требует пересмотра теста/отметки. Linux-установка локально не исполнялась;
платформенные ветви lock должны пройти CI перед выпуском.

В песочнице Codex Chromium и стандартный системный Temp pytest потребовали
разрешения. Для временных Python-файлов внутри workspace подходит уникальный
`--basetemp=.cache/<имя-прогона>`; не указывать туда пользовательские данные,
поскольку pytest очищает этот каталог.

## R1 — завершён, 24.09.2026

Пользователь подтвердил выбор GPT-6 Astra, High. Модель автоматически не менялась.

Выполненные изменения:

1. `15af061` / F01: заголовки, теги и подсказки выводятся безопасно; формулы очищаются
   после восстановления из Markdown. Исправлены аналогичные sinks диалогов/источников;
   DOMPurify обновлён до 3.4.16. Проверяется и старый HTML при отображении.
2. `803b3c3` / F02: сохранения сериализованы, payload копируется; новые изменения
   досохраняются, поздний ответ не переключает документ и не стирает dirty.
   Новые заметки участвуют в autosave, событие noteSaved отделено от noteLoaded.
3. `c37c2a7` / F03: FK включены на каждом соединении; каскады удаления и повторное
   использование ID проверены на файловой SQLite. Обнаруженные сироты блокируют
   открытие БД; скрипт repair_db_copy.py исправляет только новую копию с карантином
   исходных строк. В снимке пользовательской БД — 0 нарушений FK, ремонт не требовался.
4. `f733a19` / F04 и часть F11: одиночная генерация возвращает in_progress,
   прежний draft нормализуется на границе API и загрузки в UI. Обновление и
   инвалидация существующего блока отмечают документ изменённым.
5. `8928dfa` / F05: встроенные PNG/JPEG/WebP до 2 МиБ сохраняются и отображаются;
   проверяются base64, MIME и сигнатура. Неподдерживаемые src отклоняются при записи,
   при публичном чтении старого содержимого очищаются. Атрибуты событий запрещены.

Итоговые проверки:

| Проверка | Результат |
|---|---|
| Python pytest | 97 passed, без xfail |
| npm test | 11 passed, без todo |
| npm run test:browser | 6 сценариев + общий harness, 7 passed |
| npm run check:js | 57 файлов, синтаксис корректен |
| pip check / git diff --check | Без ошибок |
| Локальный сервер из .venv, порт 8080 | /health, / и GET /api/dialectics — HTTP 200 |
| Рисунок | create → update → checkpoint → restore → публичная страница; ошибка 422 не меняет документ |

Тесты запускаются на временных БД, без вызовов ИИ-провайдеров. Браузерные тесты
проверяют настоящие DOMPurify/marked и модули рендера в изолированном Chromium;
полный пользовательский E2E с CDN ещё относится к R5. Защита от конфликтов между
двумя вкладками (server revision/409), миграции схемы и sharing относятся к R3.
Атомарная замена семейства шагов и защита от поздней генерации относятся к R4.
Лимит 2 МиБ применяется к отдельному изображению; общий лимит запросов — R2.

## R2 — завершён, 24.09.2026

Коммиты: `9c1b2dc` (зависимости/аудит), `b10f7fb` (границы приложения и тесты).

Решение и эксплуатационные ограничения: [access-and-import-boundaries.md](access-and-import-boundaries.md).

- F06: личный режим ограничен loopback/Host и защищён от браузерных cross-origin
  записей. Demo использует серверный реестр случайных bearer-сессий, срок 7 дней,
  лимиты числа сессий и допуска записи по объёму БД. Проверена изоляция объектов.
- F08: ограничены HTTP-тело, файлы и URL; PDF вынесен в процесс с пределами памяти,
  страниц, времени и параллелизма. DNS проверяется перед каждым переходом, соединение
  идёт на проверенный IP, исходное имя сохраняется для Host/TLS.
- F09: отключение проверки TLS больше не допускается. OAuth/API используют один
  SSLContext. Официальный CA настроен локально; оба TLS handshake прошли без ключей.
- F18: доверие XFF/HTTPS-схеме только от настроенной цепочки IP/CIDR; run.py отключает
  предварительное доверие Uvicorn. Общая/session/IP квота атомарно резервируется в SQLite.
- F20: обновлены уязвимые runtime/dev-пакеты; аудит всех 75 Python lock-версий,
  включая другие ОС, и npm lock — 0 известных уязвимостей, пропусков Python нет.
- Для безопасной очистки demo добавлены leases и освобождение engine; один процесс
  на каталог обеспечен блокировкой. Неизвестные старые demo-БД не удаляются.

Проверки: **115 Python-тестов**, 11 JS-тестов, синтаксис 57 JS-файлов;
`pip check` и `git diff --check` без ошибок. PDF тестируется настоящим worker на
Windows, включая извлечение текста; TLS-тест использует локальный недоверенный
сертификат. Источник сертификата и результаты аудита закреплены в документации.
После перезапуска на порту 8080 `/health`, `/` и список конспектов вернули 200;
запрос к личной БД с поддельным Forwarded-заголовком — 403.

Границы: приватные аккаунты/owner_id и миграция старых demo — отдельное решение R3;
общий межсессионный sharing ещё не исправлен. Linux исполнение проверит CI.
Demo-порог БД — допуск записи, не жёсткая квота диска. Полный браузерный lock — R5.
Изменения не публиковались и не разворачивались на production.

## R3 — завершён, 24.09.2026

Решения и восстановление: [storage-and-sharing.md](storage-and-sharing.md).

- Версионированная транзакционная миграция 001 до запуска приложения, резервный
  снимок и остановка при неполной/повреждённой/будущей схеме.
- Контракт Note/Block с schema_version, стабильными ID и revision. Конфликт
  сохранения — 409; локальные правки сохраняются, UI предлагает отдельную копию.
- Исправлены PATCH null/absent, enum/диапазоны, дублирование и обе стороны связи.
  История проверяет принадлежность документа; восстановление требует revision.
- История, категории, связи и sharing вынесены из общего сервиса.
- Публичный live view работает без сессии читателя, показывает ручные блоки
  и секции; отзыв/удаление/истечение сессии закрывают доступ.
- Demo: last_seen, ограниченный LRU-кеш engines, сериализация запросов к одной БД,
  проверенная очистка после освобождения соединений. Подготовлен отдельный
  офлайн-перенос старой demo-базы в выданную сервером сессию.
- Загрузка примеров при старте больше не перезаписывает существующие документы.

Проверки: **133 Python-теста**, **12 JS-тестов**, **7 браузерных проверок**
(6 сценариев и общий harness), синтаксис 57 JS-файлов. Полные браузерные E2E
редактора остаются R5; новых зависимостей нет.

Рабочая БД мигрирована после проверки копии. **12 конспектов и 74 версии**:
текст и дополнительные поля сохранены, категории и связи идентичны снимку.
Свежая проверенная копия: backups/before-r3-restart-20260924.db; автоматический
снимок также находится в data/migration-backups. Сервер перезапущен на 8080;
главная страница, /health и список конспектов возвращают HTTP 200.

Production не затронут. Неизвестные legacy demo-файлы не перенесены:
владельца должен подтвердить оператор. Решение оставить БД на сессию
сохраняет требование одного процесса; аккаунты с постоянным owner_id не добавлены.

## R4 — завершён, 24.09.2026

Контракты и ограничения: [generation-contracts.md](generation-contracts.md).

- Генерация разделена на Planner, TextGenerator, Judge, RepairPolicy,
  Orchestrator и ProviderAdapter; JSON/SSE используют одну операцию.
- Applicability проверяется до текста. Строгие DTO и JSON-парсер исключают
  ложный успех при строковом boolean, пустом объекте или тексте ошибки.
- Исправление получает замечание, текущий текст и свежие зависимости;
  последующие шаги перестраиваются в порядке зависимостей.
- Общий контекст ограничивает время, физические LLM-вызовы, резерв токенов
  и параллелизм; fallback/retry учитываются. Disconnect отменяет работу.
- Маршруты различают предпочтения и разрешённый набор; независимость
  проверки учитывает фактическую модель. Ошибка проверки остаётся unavailable.
- SSE имеет run_id, sequence и terminal; обрыв не становится completed
  и не попадает в кэш. Состояния генерации отделены от оценки судьи.
- Редактор атомарно заменяет семейства блоков в обе стороны и инвалидирует
  зависимые шаги. Ручная правка во время запроса сохраняется; результат ИИ
  можно сохранить копией. Partial применяется только после подтверждения.
- Исправлен разбор экранированной нумерации Markdown. Авторские prompts
  и методология не менялись; схема БД остаётся версии 1.

Проверки: **174 Python-теста**, **27 JS-тестов**, **7 браузерных проверок**
(6 сценариев и harness), синтаксис **58 JS-файлов**; `pip check` и
`git diff --check` без ошибок. Проверки работают на временных БД и mock LLM.

Контрольный набор — **80 офлайн-прогонов**: полный успех, отказ применимости,
rate limit/fallback, неправильный JSON судьи, пустой ответ, timeout, обрыв
потока, отмена. Ложных completed/passed в сценариях сбоя нет.
Полный успех: p50 **280,226 мс**, p95 **280,898 мс**, **18 вызовов**;
отказ применимости: **1 вызов**. Все задержки и usage искусственные;
скорость, денежная стоимость и качество реальных моделей не измерялись.
Полные результаты: [benchmark JSON](benchmarks/r4-generation-2026-09-24.json).

Локальный сервер перезапущен на 8080 после проверок. Production не затронут.

## R5 — завершён, 24.09.2026

- Выделены NoteStore и SaveCoordinator; прямые изменения документа запрещены.
  Стабильные представления блоков предотвращают устаревание обработчиков после save.
- Разделены noteOpened/noteSaved; checkpoint учитывает активное редактирование,
  простой, навигацию и изменения во время запроса независимо от autosave.
- Общий Lifecycle освобождает listeners, timers, TipTap, Fabric и график;
  окна парсеров освобождают document listeners и микрофон при закрытии.
- Зависимости и шрифты собираются локально с хешами; inline JS/importmap/CDN
  убраны, script CSP не требует unsafe-inline/unsafe-eval. Обновлены TipTap/Fabric.
- Включены ESLint и проверка типов API DTO. Добавлен браузерный smoke с отдельной
  БД и mock SSE: ручной ввод, сохранение/reload, версии, sharing, формулы,
  рисунки, графики, повторные открытия, клавиатура и экран 390×844.

Проверки: **176 Python**, **36 JS**, **15 browser** (13 сценариев + 2 harness),
lint, API typecheck и синтаксис **64 JS-файлов**. `npm audit`: 0 известных
уязвимостей. Повторная сборка даёт тот же manifest; `git diff --check` пройден.
Архитектура и границы: [editor-architecture.md](editor-architecture.md).
Схема БД и авторские prompts на этом этапе не менялись.

## R6 — выпуск, уборка и сопровождение

Реализация и локальные регрессии проверены; идёт репетиция чистой установки.

- SQLite migration 002: общие стикеры входят в save/history/export, старый
  sticker_text сохраняется и переносится. Исправлены безопасный рендер стикеров,
  закрепление/открепление версии и частичное обновление категории.
- Исправлены выход панели стикеров за экран, переполнение панели действий
  при ширине 800 px и слишком строгий общий CRUD rate limit. Лимиты ИИ отдельные.
- CI на PR/main проверяет build, Python, JS, browser и зависимости. Релиз содержит
  точный SHA и хеши; отдельный venv и миграция проверяются на копии данных.
  Готовность API закрыта до сверки SHA, rollback возвращает code/venv/SQLite.
- Добавлены структурированные события без секретов и текста документов.
- Удалены дублирующие DTO, два неиспользуемых провайдера, RAG-заглушка и
  неиспользуемые параметры. Старые endpoints помечены deprecated после поиска
  потребителей в проекте; внешние потребители неизвестны, маршруты сохранены.
- Согласованы README, .env.example, API/package version 0.12.0 и changelog.
  Добавлены четыре ADR и [инструкция выпуска](deployment.md).

Проверки: **196 Python**, **36 JS**, **16 browser** (14 сценариев + 2 harness),
lint/typecheck и синтаксис **64 JS-файлов**. Python/npm audit: известных
уязвимостей нет. Сбои install/preflight/migration/restart/health и повтор того же
SHA проверены с реальным SQLite snapshot/restore и адаптером systemd/pip.

Записи R0–R5 выше — архив этапов на момент их завершения. Например, сохранённый
в R0 старый venv и долг стикеров R5 не описывают конечное состояние R6.
Linux systemd/SSH и публичный HTTPS требуют отдельной проверки на staging;
production этим рефакторингом не обновлялся.
