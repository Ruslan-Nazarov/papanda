# Рефакторинг: состояние этапов

## R0 — завершён, 24.09.2026

Основа: аудит от 24.09.2026 для `main@498dc03`.
Рабочая ветка: `refactor/staged-hardening`.

Сделано:

- Ручной режим и поведение onboarding сохранены отдельным коммитом `c88df49`.
- Создано рабочее `.venv` на Python 3.12; старый `venv` сохранён.
- Runtime/dev зависимости разделены; universal locks для Python 3.12 включают
  транзитивные версии, платформенные условия и хеши. Прямые версии приложения
  сохранены; известные advisories закрываются на следующем этапе безопасности.
- Добавлены package.json/package-lock.json с зафиксированным Puppeteer и команды
  проверки JS, логических и браузерных тестов. CDN runtime пока не переносился.
- Добавлены фикстура с настоящим lifespan/get_db и временной SQLite, тест повторного
  открытия engine, регрессии FK/истории, save race и небезопасного DOM-рендера.
- Тесты изолируют пути данных и API-ключи; cleanup фикстур выполняется в finally.
- Добавлен SQLite backup API с проверкой целостности и восстановлением в памяти.
  Локальный снимок: `backups/before-r1-20260924.db`, исключён из Git.
- README, .env.example и run.bat соответствуют новой среде. CI тестов использует
  dev-lock; production-команда установки использует runtime-lock.

Проверено на Windows, Python 3.12.14, Node 24.15.0:

| Проверка | Результат |
|---|---|
| Установка Python по хешам в отдельное окружение | Успешно |
| `pip check` | No broken requirements found |
| `pytest` | 82 passed, 2 xfailed |
| `--runxfail` для SQLite-регрессий | Оба дефекта падают на ожидаемых проверках |
| `npm ci --ignore-scripts` | Успешно, 27 пакетов |
| `npm run check:js` | 56 файлов, ошибок синтаксиса нет |
| `npm test` | 1 passed, 2 todo (известные save race) |
| `npm run test:browser` | Браузерный harness прошёл, 2 todo (известные DOM-инъекции) |
| Снимок текущей БД | Целостность и пробное восстановление проверены |

`xfail`/`todo` — открытые дефекты, не успешные исправления. В R1 убрать каждую
отметку вместе с исправлением. Python xfail строгий: неожиданное прохождение
требует пересмотра теста/отметки. Linux-установка локально не исполнялась;
платформенные ветви lock должны пройти CI перед выпуском.

В песочнице Codex Chromium и стандартный системный Temp pytest потребовали
разрешения. Для временных Python-файлов внутри workspace подходит уникальный
`--basetemp=.cache/<имя-прогона>`; не указывать туда пользовательские данные,
поскольку pytest очищает этот каталог.

## R1 — завершён, 24.09.2026

Пользователь подтвердил выбор GPT-6 Astra, High. Модель автоматически не менялась.

Выполненные изменения:

1. `15af061` / F01: заголовки, теги и подсказки выводятся безопасно; формулы очищаются
   после восстановления из Markdown. Исправлены аналогичные sinks диалогов/источников;
   DOMPurify обновлён до 3.4.16. Проверяется и старый HTML при отображении.
2. `803b3c3` / F02: сохранения сериализованы, payload копируется; новые изменения
   досохраняются, поздний ответ не переключает документ и не стирает dirty.
   Новые заметки участвуют в autosave, событие noteSaved отделено от noteLoaded.
3. `c37c2a7` / F03: FK включены на каждом соединении; каскады удаления и повторное
   использование ID проверены на файловой SQLite. Обнаруженные сироты блокируют
   открытие БД; скрипт repair_db_copy.py исправляет только новую копию с карантином
   исходных строк. В снимке пользовательской БД — 0 нарушений FK, ремонт не требовался.
4. `f733a19` / F04 и часть F11: одиночная генерация возвращает in_progress,
   прежний draft нормализуется на границе API и загрузки в UI. Обновление и
   инвалидация существующего блока отмечают документ изменённым.
5. `8928dfa` / F05: встроенные PNG/JPEG/WebP до 2 МиБ сохраняются и отображаются;
   проверяются base64, MIME и сигнатура. Неподдерживаемые src отклоняются при записи,
   при публичном чтении старого содержимого очищаются. Атрибуты событий запрещены.

Итоговые проверки:

| Проверка | Результат |
|---|---|
| Python pytest | 97 passed, без xfail |
| npm test | 11 passed, без todo |
| npm run test:browser | 6 сценариев + общий harness, 7 passed |
| npm run check:js | 57 файлов, синтаксис корректен |
| pip check / git diff --check | Без ошибок |
| Локальный сервер из .venv, порт 8080 | /health, / и GET /api/dialectics — HTTP 200 |
| Рисунок | create → update → checkpoint → restore → публичная страница; ошибка 422 не меняет документ |

Тесты запускаются на временных БД, без вызовов ИИ-провайдеров. Браузерные тесты
проверяют настоящие DOMPurify/marked и модули рендера в изолированном Chromium;
полный пользовательский E2E с CDN ещё относится к R5. Защита от конфликтов между
двумя вкладками (server revision/409), миграции схемы и sharing относятся к R3.
Атомарная замена семейства шагов и защита от поздней генерации относятся к R4.
Лимит 2 МиБ применяется к отдельному изображению; общий лимит запросов — R2.

## Далее: R2 — доступ и внешние входы

Модель остаётся GPT-6 Astra, High; переключение для R2 не требуется.
Следующие этапы: R3 хранение/API/sharing, R4 генерация, R5 интерфейс, R6 выпуск
и уборка. Перед R4 рекомендован переход на Astra XHigh для решений об архитектуре.
